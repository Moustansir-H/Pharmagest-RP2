<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>
<AnchorPane xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="mcci.businessschool.bts.sio.slam.pharmagest.commande.controleur.CreationCommandeControleur">
    <VBox spacing="10" alignment="CENTER" prefWidth="600" prefHeight="400">
        <!-- Titre -->
        <Label text="📋 Création d'une nouvelle commande" style="-fx-font-size: 18px; -fx-font-weight: bold;"/>

        <!-- Sélection du fournisseur -->
        <HBox spacing="10" alignment="CENTER">
            <Label text="Fournisseur :"/>
            <ComboBox fx:id="comboFournisseurs" onAction="#afficherMedicamentsSousSeuil" prefWidth="200"/>
        </HBox>

        <!-- Table des médicaments sous seuil -->
        <TableView fx:id="tableMedicaments" prefWidth="500" prefHeight="200">
            <columns>
                <TableColumn fx:id="colMedicament" text="Médicament" prefWidth="150"/>
                <TableColumn fx:id="colStockActuel" text="Stock Actuel" prefWidth="100"/>
                <TableColumn fx:id="colSeuil" text="Seuil" prefWidth="50"/>
                <TableColumn fx:id="colQuantiteACommander" text="Quantité à Commander" prefWidth="150"/>
                <TableColumn fx:id="colQteMax" text="Quantité maximale" prefWidth="130"/>
            </columns>
        </TableView>

        <HBox alignment="CENTER_RIGHT" spacing="10">
            <Label text="Montant total : "/>
            <Label fx:id="lblMontantTotal" text="0.0 €" style="-fx-font-weight: bold; -fx-font-size: 14px;"/>
        </HBox>

        <!-- Bouton pour créer la commande -->
        <Button fx:id="btnCreerCommande" text="Créer la commande" onAction="#creerCommande"
                style="-fx-font-size: 14px;"/>

        <!-- Bouton Retour -->
        <Button fx:id="btnRetour" text="Retour" onAction="#retourApprovisionnement"
                style="-fx-font-size: 14px; -fx-background-color: red; -fx-text-fill: white;"/>
    </VBox>
</AnchorPane>
